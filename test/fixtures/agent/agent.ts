// Plain-object default export — the loader only reads `.default`, and keeping
// it import-free makes the fixture trivial for the test runner to load.
export default {
  model: "claude-sonnet-4-5",
  name: "fixture-agent",
  description: "A fixture agent used by the test suite.",
};
